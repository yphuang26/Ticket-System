## test_nginx_rate_limit.js 說明

驗證 Nginx `limit_req` 限流機制是否正確觸發 429。

### 測試情境

- **VU 數**：100，持續 60s
- **目標**：`http://nginx`（經 Nginx，不直連後端）
- **壓測前**：呼叫 `web:8000/admin/reset?stock=100000` 重置庫存（繞過 Nginx，確保庫存充足）

### 注意事項

k6 container 內所有 VU 共用同一來源 IP，Nginx 的 per-IP rate limit（10 r/s，burst=20）是對全部 VU 共享的，不是每個 VU 各自享有 10 r/s。因此 100 VU × no sleep 必定觸發大量 429，符合「驗證限流機制有效」的目的，但無法模擬「多個真實用戶各自觸發自身 rate limit」的場景。

### Thresholds

| 指標                    | 條件              | 說明                                          |
| ----------------------- | ----------------- | --------------------------------------------- |
| `checks`                | `rate > 0.95`     | 所有回應必須是合法狀態（200 或 429），不允許 5xx 或逾時 |
| `rate_limited_responses`| `count > 0`       | 限流機制必須實際觸發，至少出現一次 429            |
| `purchase_success_rate` | `rate > 0`        | 後端仍存活，有請求通過限流並成功                  |
| `purchase_success_rate` | `rate < 0.20`     | 限流確實壓制了成功率（理論通過上限約 620 次 / 6000 次請求） |

## 測試結果

（補充實際執行後的截圖或數據）

## 錯誤類型

| 狀態碼 | 原因                        | 是否預期 |
| ------ | --------------------------- | -------- |
| 429    | Nginx `limit_req` 限流觸發  | 預期內   |
| 200    | 通過限流，後端正常回應       | 預期內   |
| 5xx    | 後端崩潰或異常               | 非預期   |
